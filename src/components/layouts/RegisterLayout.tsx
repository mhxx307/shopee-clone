function RegisterLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <p>register layout</p>
            {children}
        </div>
    );
}

export default RegisterLayout;
